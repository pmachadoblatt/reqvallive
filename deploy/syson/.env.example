# Copie para .env nesta pasta se quiser override (opcional).
# .env local NÃO vai para o Git.

# Porta no browser / no PC (contentor continua a ouvir 8080 por dentro)
SYSON_HOST_PORT=8081

# Imagem SysON — pin da versão estável usada no mestrado
# IMAGE_TAG=eclipsesyson/syson:v2025.6.0
